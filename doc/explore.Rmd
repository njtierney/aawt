---
title: "Untitled Draft"
author: "Report Author"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r loadd}
loadd(aawt_trail)
loadd(oz_map_aawt_states)
loadd(campsite_locations)
loadd(campsite_stations)
loadd(campsite_weather)
loadd(campsite_weather_summary)
```

```{r}
ggplot() + 
  geom_sf(data = oz_map_aawt_states) +
  geom_sf(data = campsite_locations,
          colour = "red")
```

```{r}

track_centroid <- st_centroid(st_geometry(aawt_tracks))
plot(track_centroid)
plot(aawt_tracks$geometry)


```


## Analysis

## Reproducibility

<details><summary>Reproducibility receipt</summary>

```{r}
## datetime
Sys.time()

## repository
if(requireNamespace('git2r', quietly = TRUE)) {
  git2r::repository()
} else {
  c(
    system2("git", args = c("log", "--name-status", "-1"), stdout = TRUE),
    system2("git", args = c("remote", "-v"), stdout = TRUE)
  )
}

## session info
sessionInfo()
```

</details>
